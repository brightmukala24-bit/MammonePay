import Navbar from "../components/Navbar";
import BuyCard from "../components/BuyCard";
import SellCard from "../components/SellCard";

function Home() {
  return (
    <div className="page">
      <Navbar />

      <main className="container">
        <section className="hero">
          <h1>Trade USDT in Minutes.</h1>
          <p>
            MammonePay helps you buy and sell USDT quickly with transparent rates,
            secure settlement, and a clean experience on any device.
          </p>
        </section>

        <section className="cards-grid">
          <BuyCard />
          <SellCard />
        </section>
      </main>
    </div>
  );
}

export default Home;
